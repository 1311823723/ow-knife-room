替换旧规则：规则("规则 7")

目的：降低主机按住蹲下时连续生成机器人的频率。

说明：这是最小安全补丁，只在原动作后增加一个已在旧代码中出现过的 `等待(..., 无视条件)` 动作。不修改英雄、队伍、位置和机器人类型。

规则("主机生成机器人 - 防连发")
{
	事件
	{
		持续 - 全局;
	}

	条件
	{
		按钮被按下(主机玩家, 按钮(蹲下)) == 真;
	}

	动作
	{
		生成机器人(全部英雄, 对方队伍(所有队伍), -1, 主机玩家, 矢量(0, 0, 0));
		等待(0.500, 无视条件);
	}
}
